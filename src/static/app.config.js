const appConfig = {
    appName: 'Bitnaft',
    appVersion: '1.5.0',
    testVersion: '1.0.0',
    description:
        'A template build with nextJS to bootstrap projects, it also contains a set of tools and pre buit code to help developers.', // The meta description element provides a summary of a page's content that search engines include in search results.
};

export default appConfig;

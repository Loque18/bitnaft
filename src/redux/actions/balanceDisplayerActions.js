import { CHANGE_BALANCE_VISIBILITY } from '../constants';

// eslint-disable-next-line import/prefer-default-export
export const change_balance_visibility = () => ({
    type: CHANGE_BALANCE_VISIBILITY,
});

1. when user logins, the session is hashed & stored in the cookies
2. when the user wants to join a private page we load the cookie if session stil valid we store the user data in the store
3. session is accessed only from pages not components